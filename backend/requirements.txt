# Compatibility convenience for local development only.
# pyproject.toml is the sole dependency authority; this installs its runtime
# dependencies and the dev extra without duplicating or drifting version pins.
# Production deployments must use: python -m pip install -e ".[production]"
-e .[dev]
